# Dependencies
/node_modules
/.pnp
.pnp.js

# Next.js
/.next/
/out/
/build

# Production
/dist

# Debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
*.log
.lint_output.txt

# Environment files (NEVER commit these)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS files
.DS_Store
Thumbs.db
Desktop.ini
Screenshot/
/tmp/

# TypeScript
*.tsbuildinfo
next-env.d.ts

# Vercel
.vercel

# Testing
/screenshots/
/coverage/
/playwright-report/
/test-results/
# Regenerated on every visual-pipeline run — artifacts, not source.
/e2e/_screenshots/

# Misc
db-init.json
# NOTE: firebase-rules.json is intentionally tracked. It contains only
# declarative rule logic — no secrets, keys, or credentials. Keeping it in
# version control is required so security-rule changes are code-reviewed.
# (Previously gitignored, which made the entire security model invisible to git.)

# Agent/tooling artifacts
.zcode/

# Repo pollution — kept out permanently (see Phase 0 of the implementation plan)
/sat-loom-web-app v5 latest 1/
/beatdrop---south-indian-party/
/plans/
/archive.html
/archive_meta.json
